import { config } from '.';

const BASE_URL_USERS = config.BASE_URL_USERS;
const BASE_URL_MOVIES = config.BASE_URL_MOVIES;
const BASE_URL_OFFERS = 'http://localhost:8002';

export { BASE_URL_USERS, BASE_URL_MOVIES, BASE_URL_OFFERS };
